import { StyleSheet } from "react-native"

// This is a simplified Android-specific implementation that doesn't render anything
// to prevent the double tab bar issue
export default function TabBarBackground() {
  return null
}

const styles = StyleSheet.create({})
